"""Agents for the news app."""

from mlango.agents import Agent, tool  # noqa: F401

# @tool
# def my_tool(query: str) -> str:
#     """What the tool does — the model reads this.
#
#     Args:
#         query: What to look for.
#     """
#     return "..."
#
#
# class MyAgent(Agent):
#     """One line describing the agent's job."""
#
#     class Meta:
#         system = "You are ..."
#         tools = [my_tool]
